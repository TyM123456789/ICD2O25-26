# from math import pi
# radius = 4.0
# area = pi *radius**2
# print ("The area of a circle with the radius "+ str(radius) + " is " + str(area))

from math import pi
radius = 4.0
area = pi *radius**2
print ("The area of a circle with the radius "+ str(radius) + " is " + str(round(area, 2)))